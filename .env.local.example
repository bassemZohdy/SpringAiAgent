# Spring AI Agent - Local Development Configuration
# Copy this file to .env.local for local LM Studio testing

# LM Studio Configuration
# Make sure LM Studio is running on localhost:1234 with OpenAI-compatible API enabled
OPENAI_API_KEY=lm-studio
OPENAI_BASE_URL=http://localhost:1234/v1

# Model Configuration for Local Testing
# Use the exact model name as shown in LM Studio (e.g., gpt-oss-mini, llama-2-7b-chat, etc.)
AI_MODEL=gpt-oss-mini

# Application Configuration for Local Development
SPRING_PROFILES_ACTIVE=dev
SERVER_PORT=8080
UI_PORT=4200

# Enhanced Logging for Local Testing
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_AI_DEMO=DEBUG
LOGGING_LEVEL_SPRINGFRAMEWORK_AI=DEBUG

# LM Studio Setup Instructions:
# 1. Install and start LM Studio
# 2. Download a compatible model (e.g., gpt-oss-mini or any OpenAI-compatible model)
# 3. Start the Local Server in LM Studio on port 1234
# 4. Enable "OpenAI Compatible" mode in LM Studio settings
# 5. Copy this file to .env.local and adjust AI_MODEL to match your loaded model
# 6. Run the Spring AI Agent: cd spring-ai-agent && mvn spring-boot:run